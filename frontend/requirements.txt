flet==0.86.0
flet-desktop==0.86.0
flet-map==0.86.0
httpx>=0.27.0
pydantic>=2.0
python-jose[cryptography]>=3.3.0
reportlab>=4.0.0
pywin32>=306
openpyxl>=3.1.0
pypdfium2>=4.30.0

# Usadas em runtime e antes NAO declaradas: vinham de carona nas dependencias
# opcionais que o .spec seguia por engano. Sem elas o app empacotado abre a
# janela e quebra calado (build windowed nao tem stdout).
python-dotenv>=1.0      # config/local_settings.py, no import do app
qrcode>=7.4             # QR do KuDE (services/pdf_generation/kude.py)
requests>=2.31          # adapter SIFEN (services/sifen_adapter/adapter.py)

lxml>=5.0
